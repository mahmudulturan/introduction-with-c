#include <stdio.h>
int main()
{
    // int rahim;
    // float karim;
    // char charecter;
    // scanf("%d %f %c", &rahim, &karim, &charecter);
    // printf("%d %0.1f %c", rahim, karim, charecter);

    int a, b;
    scanf("%d%% %d%%", &a, &b);
    printf("%d%% %d%%", a, b);
    return 0;
}