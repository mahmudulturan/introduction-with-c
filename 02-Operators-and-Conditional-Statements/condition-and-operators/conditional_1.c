#include <stdio.h>

int main()
{
    int a;
    scanf("%d", &a);
    if (a == 100) // if the value of a and 100 are equal then it will go the if block otherwise it will go to the else block
    {
        printf("Burger khabo");
    }
    else
    {
        printf("kichu khabo na");
    }
    return 0;
}