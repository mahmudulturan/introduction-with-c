#include <stdio.h>
int main()
{

    // to print integer
    int theNumber;         // declare the variable;
    theNumber = 4554;      // assign the value;
    int rahim = 4465456 , karim =544; // intialize a variable;
    printf("%d %d", rahim, karim);

    // to print floating
    float theFloat = 2.56454;
    printf("%0.1f", theFloat);

    // to print characters
    char theChar = 'f';
    printf("%c", theChar);

    return 0;
}